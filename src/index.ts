import Config from './utils/config';
import exporter from './exporter';

// TODO: Provide a way so that, user can change the config, for what they want to capture
// Instantiate default Config
const config = new Config();

// TODO: Need to decide what are the things we need to export
export default exporter;